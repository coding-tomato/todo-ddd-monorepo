{
  "name": "text-list-manager",
  "private": true,
  "scripts": {
    "build": "pnpm --filter @repo/core build && pnpm -r --filter !@repo/core build",
    "test": "pnpm -r test",
    "dev:react": "pnpm --filter @repo/core build && pnpm --filter react-app dev",
    "dev:vanilla": "pnpm --filter @repo/core build && pnpm --filter vanilla-app dev",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.11"
  }
}
